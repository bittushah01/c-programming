#include<stdio.h>

int main()

{
    int a, b ,c , d ;

    a = 5 * 2 - 2 * 3 ;
    printf("so the value of a:%d\n",a);
     
    b = 5 * 2 / 2 * 3 ;
    printf("so the value of b:%d\n",b);

    c = 5 * (2 / 2) * 3 ;
    printf("so the value of c:%d\n",c);

    d = 5 + 2 / 2 * 3 ;
    printf("so the value of d:%d\n",d);
     return 0;

}