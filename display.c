#include<stdio.h>

void main()
{
    int a , b;
    printf("\n enter two number",a,b);
    scanf("%d%d",&a,&b);

    if(a>b)
    {

    
    printf("\n %d is larger than %d",a,b);
    }
    else{

    printf("\n %d is larger than %d",b,a);
    }
}