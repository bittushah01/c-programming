#include <stdio.h>

int main()
{

    int a;
    int b;
    int sub;

    printf("plz enter the value of a:");
    scanf("%d", &a);
    printf("plz enter the value of b :");
    scanf("%d", &b);
    sub = a - b;
    printf("finding  the value sub: %d", sub);

    return 0;
}